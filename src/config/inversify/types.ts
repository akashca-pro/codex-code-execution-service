const TYPES = {

    ICacheProvider : Symbol.for("ICacheProvider"),

    // Kafka Manager
    KafkaManager : Symbol.for("KafkaManager"),
    
    // Execution Service
    IExecutionService : Symbol.for("IExecutionService"),

    ContainerPool : Symbol.for("ContainerPool"),

    Runner : Symbol.for("Runner"),

}

export default TYPES